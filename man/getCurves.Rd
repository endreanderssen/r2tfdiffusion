% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/r2tfOptimizedDiffusion.R
\name{getCurves}
\alias{getCurves}
\title{Extract Diffusion Curves for a Specific Receptor-TF Pair}
\usage{
getCurves(X, receptor = "LY96", TF = "NFKB1", samples = NULL)
}
\arguments{
\item{X}{A list containing \code{diffCurves} (signal propagation data) and \code{rowAnnotation} (sample metadata).}

\item{receptor}{Character, the receptor of interest (default: \code{"LY96"}).}

\item{TF}{Character, the transcription factor of interest (default: \code{"NFKB1"}).}

\item{samples}{Character vector specifying sample IDs to extract (default: \code{NULL}, which includes all available samples).}
}
\value{
A list with:
\itemize{
\item \code{diffCurves}: A subset of the diffusion curves matrix corresponding to the specified receptor-TF pair and samples.
\item \code{rowAnnotation}: A subset of the row annotation dataframe for the selected samples.
}
}
\description{
This function retrieves diffusion curves for a specified receptor-transcription factor (TF) pair
from a previously computed diffusion analysis.
}
\examples{
# Example assuming `X` is a list with precomputed diffusion curves
selected_curves <- getCurves(X, receptor = "TLR4", TF = "RELA", samples = c("Sample1", "Sample2"))
str(selected_curves)  
}
